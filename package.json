{
  "name": "ashercarlow-monorepo",
  "version": "1.0.0",
  "description": "Bun-based monorepo: backend + resume, wedding, and starwars frontends.",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "start": "bun run apps/backend/src/index.ts",
    "dev": "bun --hot apps/backend/src/index.ts",
    "build:resume": "cd apps/resume && bun run build",
    "build:wedding": "cd apps/wedding && bun run build",
    "build:starwars": "cd apps/starwars && bun run build",
    "build:frontends": "bun run build:resume && bun run build:wedding && bun run build:starwars"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.3"
  }
}
